# WebRTC and HTTP server (Python 3.7 compatible; matches the frankmocap conda env)
# NOTE: aiohttp 3.9+ drops Python 3.7 support.
aiohttp>=3.8.6,<3.9
# NOTE: newer aiortc/av releases may drop Python 3.7; pin to known-good range.
aiortc==1.5.0
av==10.0.0

# Computer vision and image processing
opencv-python>=4.5.0,<5
numpy>=1.19.0,<1.22
Pillow>=8.0.0,<10

# Deep learning (PyTorch)
# Note: Install PyTorch with CUDA support from https://pytorch.org/
# Example for CUDA 11.8:
# torch>=2.0.0
# torchvision>=0.15.0
# For CPU-only:
# torch>=2.0.0+cpu
# torchvision>=0.15.0+cpu

# FrankMocap dependencies (check frankmocap/requirements.txt for exact versions)
# These are typically included when you install FrankMocap:
# smplx>=0.1.28
# trimesh>=3.9.0
# pyrender>=0.1.45
# chumpy>=0.70

# Optional but recommended
python-dotenv>=0.21.0,<2
